name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  math:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run math unit tests
        working-directory: tests
        run: make test
